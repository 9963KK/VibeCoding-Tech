# API 文档

> **文档ID**: P-001
> **维护者**: 后端团队
> **最后更新**: YYYY-MM-DD

---

## 📌 文档说明

**是什么**：记录项目所有 REST API 端点的详细信息。

**怎么用**：
- 开发前端时：查询可用接口
- 联调测试时：确认请求/响应格式
- 编写测试时：参考示例数据

**更新时机**：
- 新增 API 端点时
- 修改请求/响应格式时
- 变更认证方式时

---

## 1. 概述

### 1.1 基础信息

| 项目 | 值 |
|------|-----|
| Base URL | `https://api.example.com/v1` |
| 认证方式 | Bearer Token (JWT) |
| 响应格式 | JSON |
| API 版本 | v1 |

### 1.2 通用响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 1.3 错误码说明

| HTTP 状态码 | 业务码 | 说明 |
|-------------|--------|------|
| 200 | 0 | 成功 |
| 400 | 1001 | 参数错误 |
| 401 | 1002 | 未认证 |
| 403 | 1003 | 无权限 |
| 404 | 1004 | 资源不存在 |
| 500 | 1005 | 服务器错误 |

---

## 2. 认证模块 (Auth)

### 2.1 用户注册

**POST** `/auth/register`

**请求体**：
```json
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "displayName": "John Doe"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": "uuid-xxx",
    "email": "user@example.com"
  }
}
```

### 2.2 用户登录

**POST** `/auth/login`

**请求体**：
```json
{
  "email": "user@example.com",
  "password": "SecurePass123!"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIs...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIs...",
    "expiresIn": 900
  }
}
```

---

## 3. 用户模块 (User)

### 3.1 获取用户信息

**GET** `/users/:id`

**请求头**：
```
Authorization: Bearer <accessToken>
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "uuid-xxx",
    "email": "user@example.com",
    "displayName": "John Doe",
    "avatarUrl": "https://..."
  }
}
```

---

## 📋 附录

### 相关文档

- [规范文档](../core/Standards.md) - 文档体系入口
- [附加材料](../core/Appendix.md) - API 设计规范（API-001~003）
